environments:
  development: {confirmation_required: false, max_log_tail: 300, timeout_seconds: 45, retention_days: 7}
  homologation: {confirmation_required: false, max_log_tail: 500, timeout_seconds: 60, retention_days: 15}
  production: {confirmation_required: true, max_log_tail: 200, timeout_seconds: 90, retention_days: 30}
  laboratory: {confirmation_required: false, max_log_tail: 200, timeout_seconds: 30, retention_days: 3}
